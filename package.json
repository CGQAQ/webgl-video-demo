{
  "name": "webgl-video",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write -u . "
  },
  "devDependencies": {
    "prettier": "^2.8.7",
    "typescript": "^5.0.0",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
